{
    "resourceType": "service",
    "collectionName": "services",
    "parentResource": "namespace",

    "resourceFields": {
        "name": {"type": "string"},
        "serviceType": {"type": "string"},
        "exposedPort": {"type": "array", "elemType": "servicePort"}
    },

    "subResources": {
        "servicePort": {
            "name": {"type": "string"},
            "port": {"type": "int"},
            "targetPort": {"type": "string"},
            "protocol": {"type": "string"}
        }
    },

    "resourceMethods": [
        { "name": "GET" },
        { "name": "DELETE" }
    ],
    "collectionMethods": [
        {
            "name": "POST",
            "parameters": {
                "name": {"type": "string", "required": true},
                "serviceType": {"type": "uint32", "required": true},
                "exposedPorts":  {"type": "array", "elemType": "container", "required": true} ,
                "labels": {"type": "map", "keyType": "string", "valueType": "string"},
                "annotations": {"type": "map", "keyType": "string", "valueType": "string"}
            }
        },
        { "name": "GET" }
    ]
}
