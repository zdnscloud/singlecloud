{
    "resourceType": "middleware",
    "collectionName": "middlewares",
    "parentResources": ["cluster"],

    "resourceFields": {
        "name": {
            "type": "enum",
            "validValues": ["monitor", "registry"]
        },
        "version": {
            "type": "string"
        },
        "configs": {
            "type": "json"
        },
        "redirectUrl": {
            "type": "string"
        },
        "status": {
            "type": "enum",
            "validValues": ["creating", "running"]
        }
    },

    "collectionMethods": ["GET", "POST"],
    "resourceMethods": ["GET"],

    "postParameters": {
        "fields": {
            "name": {
                "type": "string",
                "required": "true"
            },
            "configs": {
                "type": "json",
                "required": "true"
            }
        }
    }
}