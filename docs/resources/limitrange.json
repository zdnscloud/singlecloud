{
    "resourceType": "limitrange",
    "collectionName": "limitranges",
    "parentResource": "namespace",

    "resourceFields": {
        "name": {"type": "string"},
        "containerLimit": {"type": "containerLimitRange"}
    },

    "subResources": {
        "containerLimitRange": {
            "max": {"type": "map", "keyType": "containerResourceName", "valueType": "string"},
            "min": {"type": "map", "keyType": "containerResourceName", "valueType": "string"}
        },

        "containerResourceName": { "type": "enum", "validValues": ["cpu", "memory"]}
    },

    "resourceMethods": [ "GET", "DELETE" ],
    "collectionMethods": [ "GET", "POST"],

    "postParameters": {
        "fields": {
            "name": {"type": "string", "required": true},
            "containerLimit": {"type": "containerLimitRange", "required": true}
        },

        "subResources": {
            "containerLimitRange": {
                "max": {"type": "map", "keyType": "containerResourceName", "valueType": "string", "required": true},
                "min": {"type": "map", "keyType": "containerResourceName", "valueType": "string", "required": true}
            },

            "containerResourceName": { "type": "enum", "validValues": ["cpu", "memory"]}
        }
    }
}
